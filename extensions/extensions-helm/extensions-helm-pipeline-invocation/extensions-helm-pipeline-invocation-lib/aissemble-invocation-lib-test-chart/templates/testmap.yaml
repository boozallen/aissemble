{{- include "filegather.configmap" (list . "tester.configmap") -}}
{{- define "tester.configmap" -}}
metadata:
  name: pipeline-invocation-service
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace}}
data: {}
{{- end -}}