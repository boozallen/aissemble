apiVersion: v2
name: aissemble-configuration-store-chart
description: A Helm chart for configuration-store
# Version is automatically set by the plugin so this is only a placeholder
version: 1.0.0
# App Version is automatically set by the plugin so this is only a placeholder
appVersion: 1.0.0
sources:
  - https://github.com/boozallen/aissemble

dependencies:
  - name: aissemble-quarkus-chart
    version: 1.0.0
    repository: file://../aissemble-quarkus-chart/
