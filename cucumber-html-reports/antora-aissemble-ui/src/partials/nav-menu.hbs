{{#with page.navigation}}
<div class="nav-panel-menu is-active" data-panel="menu">
  <nav class="nav-menu">
    {{#with @root.page.componentVersion}}
    <!--

      Removed to prevent nav title from changing between different repos. We want a unified name, Solution Baseline, for all of these repos.

    <h3 class="title"><a href="{{{relativize ./url}}}">{{./title}}</a></h3>
    -->
    <!--
    Link to repo is not appropriately placed above the internal docs navigation pane.  Also, it currently only goes to a generic BAH Github page, not the baseline.
    <h3 class="title"><a href="/">Solution Baseline</a></h3>
    -->
    {{/with}}
{{> nav-tree navigation=this}}
  </nav>
</div>
{{/with}}
