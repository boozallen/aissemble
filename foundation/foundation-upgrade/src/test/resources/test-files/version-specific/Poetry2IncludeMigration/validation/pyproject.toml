# GENERATED STUB - PLEASE ***DO*** MODIFY

[project]
name = "my-pipeline"
version = "1.0.0.dev"
description = "${pipeline.description|'Description of package'}"
authors = [{name = "Your Name", email = "<you@example.com>"}]
requires-python = ">=3.9"

[tool.poetry]
include = [{path = "src/my_pipeline/generated/**/*", format = ["sdist", "wheel"]}]

[tool.poetry.dependencies]
my-project-data-records = {path = "../../my-project-shared/my-project-data-records", develop = true}
krausening = ">=20"
pyspark = "3.5.4"
jsonpickle = "^2.1.0"
pyyaml = "^6.0"

aissemble-foundation-core-python = "1.12.1"
aissemble-foundation-pdp-client-python = "1.12.1"
aissemble-extensions-data-delivery-spark-py = "1.12.1"
aissemble-foundation-data-lineage-python = "1.12.1"

confluent-kafka = { version = "2.1.1", optional = true }
kafka-python = "^2.0.2"

[tool.poetry.group.dev.dependencies]
behave = ">=1.2.6"
nose = ">=1.3.7"
ruff = "^0.11.7"
kappa-maki = "^1.0.2"

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"
